# Pinned versions — known-good combination. Edit only if you know why.
# Create env:  conda create -n baylearn-qg python=3.12 && conda activate baylearn-qg
# Install:     pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.27.2

# LLM clients (either provider works; the eval script picks via LLM_PROVIDER env)
groq==0.11.0
google-genai>=0.3.0

# BloomBERT runtime (must match the versions used to train the model)
torch==2.5.1
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.26.2

# Embeddings (example bank retrieval)
sentence-transformers==3.3.1

# Data prep + analysis
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
textstat
rank-bm25

# Optional — only used by scripts that download datasets from Kaggle
kagglehub==0.3.4
