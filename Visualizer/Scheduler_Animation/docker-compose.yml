services:
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    environment:
      SCH: ${SCH:-1}
      Q: ${Q:-3}
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    command: ["bash", "-lc", "sleep infinity"]
